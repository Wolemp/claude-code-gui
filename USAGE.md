# 使い方ガイド — Claude Code GUI

ターミナルを開かずに Claude Code をチャット感覚で使うためのデスクトップアプリです。
このガイドでは「インストール → 最初の5分 → 日常操作」を順に説明します。

機能の一覧は [README](README.md) を、起動方法の概要もそちらを参照してください。

---

## 1. 準備（初回のみ）

### CLI モード（推奨・APIキー不要）
Claude Code CLI をバックエンドに使うモード。Max プラン($100–200/月)のログインだけで使えます。

```bash
npm i -g @anthropic-ai/claude-code   # CLI をインストール
claude login                         # ブラウザでログイン（1回だけ）
```

### API モード（オプション・従量課金）
CLI を入れない場合の代替。Anthropic の API キー(`sk-ant-...`)が必要です。
[console.anthropic.com](https://console.anthropic.com/) で取得し、アプリ設定画面に貼り付けます。

---

## 2. 起動

| 環境 | 方法 |
|------|------|
| Python なし | `launch.bat` をダブルクリック（ポータブル Python を自動取得） |
| Python あり | `pip install -r requirements.txt` → `python main.py` |
| 配布用 | `build.bat` で `dist/ClaudeCodeGUI.exe` を生成 |

ネイティブウィンドウが開けば成功です。ブラウザは使いません。

---

## 3. 最初の5分

1. **プロジェクトを開く** — 左サイドバーの「プロジェクトを選択」で作業フォルダを指定。
   選んだフォルダが Claude のコンテキスト(`--add-dir`)として渡されます。
2. **モデルを選ぶ** — トップバーのドロップダウンから。新規タブは最新の **Opus 4.8** がデフォルト。
3. **Effort を選ぶ** — `Max / XHigh / High / Medium / Low` の5段階。思考の深さ⇔速度のバランス。
4. **メッセージを送る** — 下の入力欄に書いて `Enter`。改行は `Shift + Enter`。
5. **並行作業** — `+` または `Ctrl+T` で新しいタブ。タブごとに別プロジェクト・別セッション。

---

## 4. 日常操作

### ナビバー（入力欄の下）
| ボタン | ショートカット | 動作 |
|--------|---------------|------|
| ⎚ クリア | `Ctrl+L` | 表示だけ消す（会話セッションは継続） |
| ⏹ セッション終了 | `Ctrl+Shift+L` | バックエンドを停止、履歴は保持 |
| ↻ 新規セッション | `Ctrl+Shift+N` | 全リセットしてやり直し |
| Plan | — | 読み取り専用モード（変更を一切させない） |
| Accept Edits | — | ファイル編集だけ自動許可 |

### 権限のボタン
Claude が確認を求めると、CLI の選択肢をそのままボタン化して表示します。
クリックで承認/拒否、フィードバック評価にも対応します。

### タブのショートカット
| キー | 動作 |
|------|------|
| `Ctrl+T` / `Ctrl+W` | タブを開く / 閉じる |
| `Ctrl+Tab` / `Ctrl+Shift+Tab` | 次 / 前のタブ |
| ダブルクリック | タブ名を変更 |
| `Esc` | 処理中断 / パネルを閉じる |

### タブ設定（歯車アイコン）
- **Max Turns** — エージェントの最大ターン数（0 = 無制限）
- **カスタムCLIフラグ** — `--allowedTools` など任意のフラグ
- **システムプロンプト** — API モード用のカスタム指示
- **権限モード** — Default / Accept Edits / Plan / カスタム
- **Session ID** — 現在のセッション ID（読み取り専用）

---

## 5. データの保存場所

```
~/.claude-code-gui/
  config.json    # モード・APIキーなどグローバル設定
  tabs.json      # 各タブのプロジェクト・セッション・会話履歴
```

アプリを再起動しても `--resume` でタブと文脈が復元されます。

---

## 6. 困ったとき

| 症状 | 対処 |
|------|------|
| ウィンドウが開かない | `python main.py` を直接実行し、コンソールのエラーを確認 |
| `claude` が見つからない | CLI 未インストール。`npm i -g @anthropic-ai/claude-code` |
| モデルが応答しない | CLI モードは `claude login` 済みか、API モードはキーが正しいか確認 |
| 依存関係エラー | `pip install pywebview pywinpty pyte anthropic` |
| 文字化け・崩れ | ウィンドウを一度リサイズ（レスポンシブUIが再計算されます） |

---

## 7. セキュリティ

- API キーはローカルファイルにのみ保存。外部へ送信されません。
- CLI モードではキー自体が不要（Claude Code の OAuth 認証を使用）。
- ネットワーク通信は Claude API / Claude Code CLI 経由のみ。

ライセンスは MIT です。自由に使ってフィードバックをください 🙌
